jwt {
    key = "!OD%0RWT01Fkq!"
    key = ${?JWT_KEY}
}

mongo-async-driver {
  akka {
    loggers = ["akka.event.slf4j.Slf4jLogger"]
    loglevel = INFO
  }
}

mongo {
    uri = "mongodb://root:12345678@localhost/admin"
    uri = ${?MONGO_DB_URI}
    database = blog
    database = ${?MONGO_DATABASE}
}
